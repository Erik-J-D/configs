[user]
	email = erik@erik-j.de
	name = Erik Derohanian
	signingkey = 2452D4AE
[color]
	ui = true
[core]
	editor = vim
[push]
	default = simple
[url "git@github.com:"]
    insteadOf = https://github.com/
[alias]
    bclean = "!f() { git branch --merged | egrep -v '(^\\*|master|dev)' | xargs git branch -d ; }; f"
    all = "!f() { ls | xargs -I{} git -C {} $1; }; f"
    lastcommit = show --name-status HEAD
