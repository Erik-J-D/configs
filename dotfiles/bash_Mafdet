alias shwork="ssh erik@coubs.hopto.org -p 24999"

export PATH=/usr/local/cuda-8.0/bin:$PATH

export PYENV_ROOT="$HOME/.pyenv"
export PATH="$PYENV_ROOT/bin:$PATH"
if command -v pyenv 1>/dev/null 2>&1; then
  eval "$(pyenv init -)"
fi
